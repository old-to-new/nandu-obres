# Bug Fixes Post-E2E — nandu-obres Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Resoldre els 4 bugs detectats en el test E2E del 18/04/2026: redirect incorrecte en crear obra (CRÍTICO), fotos no disponibles a nova acta (HIGH), inconsistència labels tipus treballador (LOW), i navegació tab certificació (LOW).

**Architecture:**
- BUG-01 i BUG-02 modifiquen components React client-side (`ObraForm`, `NovaActaForm`) i un server action (`guardarActa`). El patró correcte ja existeix a `NovaActaForm` — cal aplicar-lo a `ObraForm`.
- BUG-03 introdueix un fitxer de constants compartit per eliminar duplicació.
- BUG-04 és una correcció del test E2E (timing issue).

**Tech Stack:** Next.js 14 App Router, Supabase SSR, React `useTransition`, Vitest, Playwright

---

## Mapa de fitxers

| Fitxer | Acció | Bug |
|--------|-------|-----|
| `src/components/obres/ObraForm.tsx` | Modificar — afegir try-catch + error state + useRouter | BUG-01 |
| `src/app/(dashboard)/obres/[id]/actions.ts` | Modificar — `guardarActa` retorna `acteId` en lloc de redirect | BUG-02 |
| `src/components/actes/NovaActaForm.tsx` | Modificar — mostrar `ActaFotosUpload` post-save + Finalitzar | BUG-02 |
| `src/lib/treballadors.ts` | Crear — constants `TIPUS_LABELS` centralitzades | BUG-03 |
| `src/components/treballadors/TreballadorCard.tsx` | Modificar — importar TIPUS_LABELS des de lib | BUG-03 |
| `src/components/actes/ActaTreballadorsEditor.tsx` | Modificar — importar TIPUS_LABELS des de lib | BUG-03 |
| `tests/nandu.spec.js` (o nou fitxer) | Modificar — fix timing BUG-04, tests regression BUG-01 i BUG-02 | BUG-04 |

---

## Task 1: BUG-01 — Fix `ObraForm` redirect (try-catch + error state)

**Root cause:** `ObraForm` crida `createObra` dins `startTransition` sense try-catch. Quan `createObra` crida `redirect()`, llança `NEXT_REDIRECT`. Sense captura, l'error es propaga com a uncaught, i el middleware el tracta com a error d'autenticació redirigint a `/login`. `NovaActaForm` té el mateix patró PERÒ amb try-catch que filtra `NEXT_REDIRECT` — funciona correctament.

**Files:**
- Modify: `src/components/obres/ObraForm.tsx`
- Test: `src/components/obres/__tests__/ObraForm.test.tsx` (crear)

- [ ] **Step 1.1: Escriure el test de component que documenta el comportament esperat**

Crear `src/components/obres/__tests__/ObraForm.test.tsx`:

```tsx
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { render, screen, fireEvent, waitFor } from '@testing-library/react'
import ObraForm from '../ObraForm'

// Mocks
vi.mock('next/navigation', () => ({
  useRouter: () => ({ push: vi.fn() }),
}))
vi.mock('@/app/(dashboard)/obres/actions', () => ({
  createObra: vi.fn(),
  updateObra: vi.fn(),
}))

import { createObra, updateObra } from '@/app/(dashboard)/obres/actions'

describe('ObraForm', () => {
  beforeEach(() => vi.clearAllMocks())

  it('crida createObra en mode creació i no mostra error si NEXT_REDIRECT', async () => {
    const NEXT_REDIRECT_ERR = new Error('NEXT_REDIRECT')
    vi.mocked(createObra).mockRejectedValue(NEXT_REDIRECT_ERR)

    render(<ObraForm />)

    fireEvent.change(screen.getByLabelText(/nom de l'obra/i), {
      target: { value: 'Test Obra' },
    })
    fireEvent.change(screen.getByLabelText(/client/i), {
      target: { value: 'Client Test' },
    })
    fireEvent.click(screen.getByRole('button', { name: /crear obra/i }))

    await waitFor(() => {
      expect(createObra).toHaveBeenCalledOnce()
    })

    // No hauria de mostrar missatge d'error per NEXT_REDIRECT
    expect(screen.queryByRole('alert')).not.toBeInTheDocument()
  })

  it('mostra missatge d'error si createObra llança error real', async () => {
    vi.mocked(createObra).mockRejectedValue(new Error('DB connection failed'))

    render(<ObraForm />)

    fireEvent.change(screen.getByLabelText(/nom de l'obra/i), {
      target: { value: 'Test' },
    })
    fireEvent.change(screen.getByLabelText(/client/i), {
      target: { value: 'Client' },
    })
    fireEvent.click(screen.getByRole('button', { name: /crear obra/i }))

    await waitFor(() => {
      expect(screen.getByRole('alert')).toBeInTheDocument()
      expect(screen.getByText(/DB connection failed/i)).toBeInTheDocument()
    })
  })

  it('crida updateObra i navega a /obres/:id en mode edició', async () => {
    const mockPush = vi.fn()
    vi.mocked(updateObra).mockResolvedValue(undefined)
    vi.mock('next/navigation', () => ({ useRouter: () => ({ push: mockPush }) }))

    const obraExistent = {
      id: 'uuid-1',
      nom: 'Obra Existent',
      client_nom: 'Client',
      linia: 'rehabilitacio' as const,
      estat: 'activa' as const,
      notes: null,
      pressupost_pdf_url: null,
      projecte_pdf_url: null,
      created_at: '2026-01-01',
    }
    render(<ObraForm obra={obraExistent} />)

    fireEvent.click(screen.getByRole('button', { name: /guardar canvis/i }))

    await waitFor(() => {
      expect(updateObra).toHaveBeenCalledWith('uuid-1', expect.any(FormData))
    })
  })
})
```

- [ ] **Step 1.2: Executar el test per confirmar que falla**

```bash
cd "nandu-obres"
npx vitest run src/components/obres/__tests__/ObraForm.test.tsx
```

Expected: FAIL (ObraForm no té try-catch ni error state)

- [ ] **Step 1.3: Aplicar el fix a `ObraForm.tsx`**

Substituir tot el contingut de `src/components/obres/ObraForm.tsx`:

```tsx
'use client'

import { useState, useTransition } from 'react'
import { useRouter } from 'next/navigation'
import { createObra, updateObra } from '@/app/(dashboard)/obres/actions'
import type { Obra, LiniaObra, EstatObra } from '@/lib/types/database'

interface Props {
  obra?: Obra
  onCancel?: () => void
}

const LINIES: { value: LiniaObra; label: string }[] = [
  { value: 'obra_nova', label: 'Obra nova' },
  { value: 'rehabilitacio', label: 'Rehabilitació' },
  { value: 'ascensors', label: 'Ascensors' },
  { value: 'altres', label: 'Altres' },
]

const ESTATS: { value: EstatObra; label: string }[] = [
  { value: 'activa', label: 'Activa' },
  { value: 'pausada', label: 'Pausada' },
  { value: 'finalitzada', label: 'Finalitzada' },
]

export default function ObraForm({ obra, onCancel }: Props) {
  const [isPending, startTransition] = useTransition()
  const [error, setError] = useState<string | null>(null)
  const router = useRouter()
  const isEditing = !!obra

  function handleSubmit(formData: FormData) {
    setError(null)
    startTransition(async () => {
      try {
        if (isEditing) {
          await updateObra(obra.id, formData)
          router.push(`/obres/${obra.id}`)
        } else {
          await createObra(formData)
          // createObra crida redirect() — no arriba aquí si va bé
        }
      } catch (err) {
        if (err instanceof Error && err.message !== 'NEXT_REDIRECT') {
          setError(err.message)
        }
      }
    })
  }

  return (
    <form action={handleSubmit} className="space-y-4">
      {error && (
        <div role="alert" className="rounded-lg bg-red-50 p-3 text-sm text-red-600">
          {error}
        </div>
      )}

      <div className="grid grid-cols-1 gap-4 sm:grid-cols-2">
        <div className="flex flex-col gap-1 sm:col-span-2">
          <label htmlFor="nom" className="text-sm font-medium text-gray-700">
            Nom de l&apos;obra <span className="text-red-500">*</span>
          </label>
          <input
            id="nom"
            name="nom"
            type="text"
            required
            defaultValue={obra?.nom ?? ''}
            placeholder="ex: Casa Puigdomenech"
            className="rounded-lg border border-gray-300 px-3 py-2 text-sm focus:border-blue-500 focus:outline-none focus:ring-1 focus:ring-blue-500"
          />
        </div>

        <div className="flex flex-col gap-1 sm:col-span-2">
          <label htmlFor="client_nom" className="text-sm font-medium text-gray-700">
            Client <span className="text-red-500">*</span>
          </label>
          <input
            id="client_nom"
            name="client_nom"
            type="text"
            required
            defaultValue={obra?.client_nom ?? ''}
            placeholder="ex: Família Puigdomenech"
            className="rounded-lg border border-gray-300 px-3 py-2 text-sm focus:border-blue-500 focus:outline-none focus:ring-1 focus:ring-blue-500"
          />
        </div>

        <div className="flex flex-col gap-1">
          <label htmlFor="linia" className="text-sm font-medium text-gray-700">
            Línia <span className="text-red-500">*</span>
          </label>
          <select
            id="linia"
            name="linia"
            required
            defaultValue={obra?.linia ?? 'obra_nova'}
            className="rounded-lg border border-gray-300 px-3 py-2 text-sm focus:border-blue-500 focus:outline-none focus:ring-1 focus:ring-blue-500"
          >
            {LINIES.map((l) => (
              <option key={l.value} value={l.value}>
                {l.label}
              </option>
            ))}
          </select>
        </div>

        <div className="flex flex-col gap-1">
          <label htmlFor="estat" className="text-sm font-medium text-gray-700">
            Estat <span className="text-red-500">*</span>
          </label>
          <select
            id="estat"
            name="estat"
            required
            defaultValue={obra?.estat ?? 'activa'}
            className="rounded-lg border border-gray-300 px-3 py-2 text-sm focus:border-blue-500 focus:outline-none focus:ring-1 focus:ring-blue-500"
          >
            {ESTATS.map((e) => (
              <option key={e.value} value={e.value}>
                {e.label}
              </option>
            ))}
          </select>
        </div>

        <div className="flex flex-col gap-1 sm:col-span-2">
          <label htmlFor="notes" className="text-sm font-medium text-gray-700">
            Notes
          </label>
          <textarea
            id="notes"
            name="notes"
            rows={3}
            defaultValue={obra?.notes ?? ''}
            placeholder="Informació addicional sobre l'obra..."
            className="rounded-lg border border-gray-300 px-3 py-2 text-sm focus:border-blue-500 focus:outline-none focus:ring-1 focus:ring-blue-500"
          />
        </div>
      </div>

      <div className="flex justify-end gap-3 border-t border-gray-200 pt-4">
        {onCancel && (
          <button
            type="button"
            onClick={onCancel}
            className="rounded-lg border border-gray-300 px-4 py-2 text-sm font-medium text-gray-700 hover:bg-gray-50"
          >
            Cancel·lar
          </button>
        )}
        <button
          type="submit"
          disabled={isPending}
          className="rounded-lg bg-blue-600 px-4 py-2 text-sm font-semibold text-white hover:bg-blue-700 disabled:opacity-50"
        >
          {isPending ? 'Guardant...' : isEditing ? 'Guardar canvis' : 'Crear obra'}
        </button>
      </div>
    </form>
  )
}
```

- [ ] **Step 1.4: Executar els tests unitaris**

```bash
npx vitest run src/components/obres/__tests__/ObraForm.test.tsx
```

Expected: tots els tests PASS

- [ ] **Step 1.5: Verificar amb Playwright (E2E)**

```bash
cd /tmp/nandu-e2e
npx playwright test final.spec.js --grep "T02.*Crear nova obra" --project=chromium
```

Expected: PASS (URL after save conté `/obres/{uuid}`, no `/login`)

- [ ] **Step 1.6: Commit**

```bash
git add src/components/obres/ObraForm.tsx src/components/obres/__tests__/ObraForm.test.tsx
git commit -m "fix: ObraForm - add try-catch for NEXT_REDIRECT in startTransition

Sense try-catch, el NEXT_REDIRECT llançat per createObra es propagava com
a uncaught error i el middleware redirigía incorrectament a /login.
Patró consistent amb NovaActaForm. Afegit error state per mostrar errors reals.
Afegida navegació explícita post-updateObra via router.push."
```

---

## Task 2: BUG-02 — Fotos disponibles a nova acta (flux 2 passos)

**Root cause:** `ActaFotosUpload` existeix i funciona al detall d'acta (`/obres/[id]/actes/[acteId]`), però no al formulari de creació (`/obres/[id]/actes/nova`). `guardarActa` crida `redirect()` i surt de la pàgina immediatament, impedint afegir fotos durant la creació. Solució: `guardarActa` retorna `acteId` i `NovaActaForm` mostra upload inline abans de finalitzar.

**Files:**
- Modify: `src/app/(dashboard)/obres/[id]/actions.ts` (guardarActa)
- Modify: `src/components/actes/NovaActaForm.tsx`
- Test: `src/app/(dashboard)/obres/[id]/__tests__/actions.test.ts` (afegir test)

- [ ] **Step 2.1: Afegir test per al nou comportament de `guardarActa`**

Afegir a `src/app/(dashboard)/obres/[id]/__tests__/actions.test.ts` (al final del fitxer existent, sense eliminar els tests actuals):

```ts
// Nota: els tests existents de guardarActa han de passar — no modificar-los
// Afegir un nou describe per al comportament post-canvi:

describe('guardarActa — retorna acteId sense redirect per acta nova', () => {
  // Mocks per a aquest describe
  const mockInsertActes = vi.fn()
  const mockDeleteTrebs = vi.fn()
  const mockInsertTrebs = vi.fn()

  beforeEach(() => {
    vi.clearAllMocks()
    // mock redirect per detectar si es crida (no hauria de cridar-se)
    vi.mocked(redirect).mockImplementation(() => { throw new Error('redirect called unexpectedly') })
  })

  it('nova acta sense treballadors: retorna acteId i NO crida redirect', async () => {
    mockFrom.mockImplementation((table: string) => {
      if (table === 'actes') return { insert: mockInsertActes }
      if (table === 'acte_treballadors') return { delete: mockDeleteTrebs }
      return {}
    })
    mockInsertActes.mockReturnValue({
      select: vi.fn().mockReturnValue({
        single: vi.fn().mockResolvedValue({
          data: { id: 'acte-uuid-new' },
          error: null,
        }),
      }),
    })
    mockDeleteTrebs.mockReturnValue({
      eq: vi.fn().mockResolvedValue({ error: null }),
    })

    const result = await guardarActa({
      obraId: 'obra-uuid',
      acteId: null,
      data: '2026-04-18',
      comentariGeneral: 'Test',
      treballadors: [],
    })

    expect(result).toEqual({ acteId: 'acte-uuid-new' })
    expect(redirect).not.toHaveBeenCalled()
  })
})
```

- [ ] **Step 2.2: Executar el test per confirmar que falla**

```bash
npx vitest run "src/app/(dashboard)/obres/\[id\]/__tests__/actions.test.ts"
```

Expected: FAIL — `guardarActa` crida `redirect` i llança error, no retorna `{ acteId }`

- [ ] **Step 2.3: Modificar `guardarActa` a `src/app/(dashboard)/obres/[id]/actions.ts`**

Localitzar les línies finals de `guardarActa` (aproximadament línies 55-65) i substituir:

```ts
// ABANS (a eliminar — les 3 últimes línies de guardarActa):
  revalidatePath(`/obres/${input.obraId}`)
  redirect(`/obres/${input.obraId}/actes/${acteId}`)
}
```

Per:

```ts
// DESPRÉS:
  revalidatePath(`/obres/${input.obraId}`)
  return { acteId }
}
```

La signatura de la funció canvia de `Promise<void>` a `Promise<{ acteId: string }>`. Actualitzar la declaració:

```ts
// ABANS:
export async function guardarActa(input: GuardarActaInput) {

// DESPRÉS:
export async function guardarActa(input: GuardarActaInput): Promise<{ acteId: string }> {
```

- [ ] **Step 2.4: Executar tests unitaris de actions**

```bash
npx vitest run "src/app/(dashboard)/obres/\[id\]/__tests__/actions.test.ts"
```

Expected: tots els tests PASS (incloent el nou)

> ⚠️ NOTA: el test existent que comprovava que `redirect` es cridava deixarà de passar — actualitzar-lo per esperar el return value `{ acteId }` en lloc de `redirect`:
>
> ```ts
> // Substituir al test existent de guardarActa (nova acta):
> // ANTES: expect(redirect).toHaveBeenCalledWith('/obres/obra-uuid/actes/acte-uuid')
> // DESPUÉS:
> const result = await guardarActa({ ... })
> expect(result).toEqual({ acteId: 'acte-uuid' })
> expect(redirect).not.toHaveBeenCalled()
> ```

- [ ] **Step 2.5: Modificar `NovaActaForm.tsx` per mostrar upload de fotos post-save**

Substituir tot el contingut de `src/components/actes/NovaActaForm.tsx`:

```tsx
'use client'

import { useState, useCallback, useTransition } from 'react'
import { useRouter } from 'next/navigation'
import type { Treballador } from '@/lib/types/database'
import { guardarActa, type TreballadorActaInput } from '@/app/(dashboard)/obres/[id]/actions'
import ActaTreballadorsEditor, { type TreballadorEditorEntry } from '@/components/actes/ActaTreballadorsEditor'
import ActaFotosUpload from '@/components/actes/ActaFotosUpload'

interface Props {
  obraId: string
  obraNom: string
  treballadorsDisponibles: Treballador[]
  initialTreballadors: TreballadorEditorEntry[]
  dataDefault: string
}

export default function NovaActaForm({
  obraId,
  obraNom,
  treballadorsDisponibles,
  initialTreballadors,
  dataDefault,
}: Props) {
  const [data, setData] = useState(dataDefault)
  const [comentariGeneral, setComentariGeneral] = useState('')
  const [treballadors, setTreballadors] = useState<TreballadorEditorEntry[]>(initialTreballadors)
  const [hasChanges, setHasChanges] = useState(false)
  const [isPending, startTransition] = useTransition()
  const [error, setError] = useState<string | null>(null)
  // Fase 2: upload fotos un cop l'acta ha estat creada
  const [savedActeId, setSavedActeId] = useState<string | null>(null)
  const router = useRouter()

  const handleTreballadorsChange = useCallback((nousTreballadors: TreballadorEditorEntry[]) => {
    setTreballadors(nousTreballadors)
    setHasChanges(true)
  }, [])

  function handleGuardar() {
    setError(null)
    startTransition(async () => {
      try {
        const result = await guardarActa({
          obraId,
          acteId: null,
          data,
          comentariGeneral,
          treballadors: treballadors.map((t): TreballadorActaInput => ({
            treballadorId: t.treballadorId,
            hores: t.hores,
            comentari: t.comentari,
            planificat: t.planificat,
          })),
        })
        setSavedActeId(result.acteId)
      } catch (err) {
        if (err instanceof Error && err.message !== 'NEXT_REDIRECT') {
          setError(err.message)
        }
      }
    })
  }

  function handleFinalitzar() {
    router.push(`/obres/${obraId}/actes/${savedActeId}`)
  }

  // Fase 2: acta creada, mostrar upload de fotos
  if (savedActeId) {
    return (
      <div className="mx-auto max-w-2xl space-y-6">
        <div>
          <h1 className="text-xl font-semibold text-gray-900">Afegir fotos</h1>
          <p className="mt-0.5 text-sm text-gray-500">
            Acta creada. Afegeix fotos del dia o finalitza directament.
          </p>
        </div>

        <div className="rounded-xl border border-gray-200 bg-white p-5">
          <ActaFotosUpload
            obraId={obraId}
            acteId={savedActeId}
            fotosInicials={[]}
          />
        </div>

        <div className="flex justify-end">
          <button
            type="button"
            onClick={handleFinalitzar}
            className="rounded-lg bg-blue-600 px-6 py-2.5 text-sm font-semibold text-white shadow-lg hover:bg-blue-700"
          >
            Finalitzar acta →
          </button>
        </div>
      </div>
    )
  }

  // Fase 1: formulari principal
  return (
    <div className="mx-auto max-w-2xl space-y-6">
      <div className="flex items-center gap-3">
        <button
          type="button"
          onClick={() => router.back()}
          className="text-sm text-gray-500 hover:text-gray-900"
        >
          ← Tornar
        </button>
        <div>
          <h1 className="text-xl font-semibold text-gray-900">Nova acta</h1>
          <p className="mt-0.5 text-sm text-gray-500">{obraNom}</p>
        </div>
      </div>

      {error && (
        <div className="rounded-lg bg-red-50 p-3 text-sm text-red-600">{error}</div>
      )}

      <div className="rounded-xl border border-gray-200 bg-white p-5">
        <div className="flex flex-col gap-1">
          <label htmlFor="data-acta" className="text-sm font-medium text-gray-700">
            Data <span className="text-red-500">*</span>
          </label>
          <input
            id="data-acta"
            type="date"
            value={data}
            onChange={(e) => { setData(e.target.value); setHasChanges(true) }}
            className="w-full max-w-xs rounded-lg border border-gray-300 px-3 py-2 text-sm focus:border-blue-500 focus:outline-none focus:ring-1 focus:ring-blue-500"
          />
        </div>
      </div>

      <div className="rounded-xl border border-gray-200 bg-white p-5">
        <h2 className="mb-4 font-semibold text-gray-900">Treballadors</h2>
        <ActaTreballadorsEditor
          treballadorsDisponibles={treballadorsDisponibles}
          initialTreballadors={initialTreballadors}
          onChange={handleTreballadorsChange}
        />
      </div>

      <div className="rounded-xl border border-gray-200 bg-white p-5">
        <div className="flex flex-col gap-1">
          <label htmlFor="comentari-general" className="text-sm font-medium text-gray-700">
            Comentari general del dia
          </label>
          <textarea
            id="comentari-general"
            rows={3}
            value={comentariGeneral}
            onChange={(e) => { setComentariGeneral(e.target.value); setHasChanges(true) }}
            placeholder="Observacions generals sobre el dia de treball..."
            className="rounded-lg border border-gray-300 px-3 py-2 text-sm focus:border-blue-500 focus:outline-none focus:ring-1 focus:ring-blue-500"
          />
        </div>
      </div>

      <div className="sticky bottom-4 flex justify-end">
        <button
          type="button"
          onClick={handleGuardar}
          disabled={isPending || !hasChanges || treballadors.length === 0}
          className="rounded-lg bg-blue-600 px-6 py-2.5 text-sm font-semibold text-white shadow-lg hover:bg-blue-700 disabled:opacity-50"
        >
          {isPending ? 'Guardant...' : 'Guardar acta →'}
        </button>
      </div>
    </div>
  )
}
```

- [ ] **Step 2.6: Executar tots els tests afectats**

```bash
npx vitest run src/components/actes/__tests__/
```

Expected: PASS

- [ ] **Step 2.7: Verificar E2E (si s'ha desplegat a preview)**

```bash
cd /tmp/nandu-e2e
npx playwright test final.spec.js --grep "T04" --project=chromium
```

Expected: test T04 passa incloent foto upload (`BUG-04b` resolt)

- [ ] **Step 2.8: Commit**

```bash
git add src/app/(dashboard)/obres/[id]/actions.ts \
        src/components/actes/NovaActaForm.tsx \
        "src/app/(dashboard)/obres/[id]/__tests__/actions.test.ts"
git commit -m "feat: nova acta - afegir fotos durant la creació (flux 2 passos)

guardarActa ara retorna acteId en lloc de fer redirect immediat.
NovaActaForm mostra ActaFotosUpload un cop guardada l'acta, amb
botó 'Finalitzar acta' que navega al detall. Consistent amb el
component existent ActaFotosUpload que ja funciona al detall d'acta."
```

---

## Task 3: BUG-03 — Centralitzar `TIPUS_LABELS` (inconsistència 'Oficial' vs 'Oficial 1a')

**Root cause:** `TreballadorCard.tsx` i `ActaTreballadorsEditor.tsx` defineixen `TIPUS_LABELS` de forma independent amb valors diferents per `oficial` ('Oficial' vs 'Oficial 1a'). Solució: centralitzar a `src/lib/treballadors.ts` i importar des dels dos components.

**Files:**
- Create: `src/lib/treballadors.ts`
- Modify: `src/components/treballadors/TreballadorCard.tsx`
- Modify: `src/components/actes/ActaTreballadorsEditor.tsx`

- [ ] **Step 3.1: Executar tests existents per confirmar l'estat de base**

```bash
npx vitest run src/components/treballadors/__tests__/TreballadorCard.test.tsx \
               src/components/actes/__tests__/ActaTreballadorsEditor.test.tsx
```

Expected: PASS (els tests existents passen)

- [ ] **Step 3.2: Crear `src/lib/treballadors.ts`**

```ts
import type { TipusTreballador } from '@/lib/types/database'

export const TIPUS_LABELS: Record<TipusTreballador, string> = {
  oficial: 'Oficial 1a',
  oficial_2a: 'Oficial 2a',
  peo: 'Peó',
  altre: 'Altre',
}
```

- [ ] **Step 3.3: Actualitzar `TreballadorCard.tsx`**

Substituir el bloc de constants local i l'import, a `src/components/treballadors/TreballadorCard.tsx`:

```tsx
// ELIMINAR aquestes línies (línies 4-9):
const TIPUS_LABELS: Record<TipusTreballador, string> = {
  oficial: 'Oficial',
  oficial_2a: 'Oficial 2a',
  peo: 'Peó',
  altre: 'Altre',
}

// AFEGIR import (línies 1-3):
import Link from 'next/link'
import type { Treballador, TipusTreballador } from '@/lib/types/database'
import { TIPUS_LABELS } from '@/lib/treballadors'
```

El fitxer resultant quedaria:

```tsx
import Link from 'next/link'
import type { Treballador, TipusTreballador } from '@/lib/types/database'
import { TIPUS_LABELS } from '@/lib/treballadors'

interface TreballadorCardProps {
  treballador: Treballador
}

export function TreballadorCard({ treballador }: TreballadorCardProps) {
  return (
    <Link
      href={`/treballadors/${treballador.id}`}
      className="block rounded-lg border border-gray-200 bg-white p-4 shadow-sm transition hover:shadow-md"
    >
      <div className="flex items-start justify-between gap-2">
        <div className="min-w-0 flex-1">
          <p className="truncate text-base font-semibold text-gray-900">
            {treballador.nom}
          </p>
          <p className="mt-0.5 text-sm text-gray-500">
            {TIPUS_LABELS[treballador.tipus]}
          </p>
          {treballador.telefon && (
            <p className="mt-1 text-sm text-gray-600">{treballador.telefon}</p>
          )}
        </div>
        {!treballador.actiu && (
          <span className="shrink-0 rounded-full bg-gray-100 px-2 py-0.5 text-xs font-medium text-gray-500">
            Inactiu
          </span>
        )}
      </div>
    </Link>
  )
}
```

- [ ] **Step 3.4: Actualitzar `ActaTreballadorsEditor.tsx`**

Substituir el bloc de constants local per l'import. A `src/components/actes/ActaTreballadorsEditor.tsx`, localitzar i eliminar:

```ts
// ELIMINAR (aproximadament línies 14-20):
const TIPUS_LABELS: Record<Treballador['tipus'], string> = {
  oficial: 'Oficial 1a',
  oficial_2a: 'Oficial 2a',
  peo: 'Peó',
  altre: 'Altre',
}
```

I afegir l'import a les primeres línies (juntament amb els imports existents):

```ts
import { TIPUS_LABELS } from '@/lib/treballadors'
```

- [ ] **Step 3.5: Executar tots els tests afectats**

```bash
npx vitest run src/components/treballadors/__tests__/TreballadorCard.test.tsx \
               src/components/actes/__tests__/ActaTreballadorsEditor.test.tsx
```

Expected: PASS

- [ ] **Step 3.6: Verificació de build (TypeScript)**

```bash
npx tsc --noEmit
```

Expected: sense errors

- [ ] **Step 3.7: Commit**

```bash
git add src/lib/treballadors.ts \
        src/components/treballadors/TreballadorCard.tsx \
        src/components/actes/ActaTreballadorsEditor.tsx
git commit -m "fix: centralitzar TIPUS_LABELS — 'oficial' ara mostra 'Oficial 1a' arreu

TreballadorCard tenia 'Oficial', ActaTreballadorsEditor tenia 'Oficial 1a'.
Font única de veritat a src/lib/treballadors.ts."
```

---

## Task 4: BUG-04 — Fix test E2E certificació (timing issue)

**Root cause:** El test E2E verifica la URL de la pàgina de certificació fent `waitForLoadState('networkidle')` després del click. La navegació client-side de Next.js es completa ràpidament i `networkidle` dispara abans que el test comprovi la URL. La solució és usar `waitForURL` o `waitForLoadState('domcontentloaded')`.

**Files:**
- Modify: `/tmp/nandu-e2e/tests/final.spec.js` (fix test T06)

> Nota: Si els tests E2E es mantenen al projecte, moure'ls a `nandu-obres/e2e/` i afegir-los al `package.json`.

- [ ] **Step 4.1: Confirmar el bug del test**

```bash
cd /tmp/nandu-e2e
npx playwright test final.spec.js --grep "T06.*accessible des del detall" --project=chromium
```

Expected: FAIL (URL no conté `/certificacio`)

- [ ] **Step 4.2: Fix el test T06 a `final.spec.js`**

Localitzar el test `'Certificació obra accessible des del detall de l\'obra'` i substituir:

```js
// ABANS:
await certLink.click();
await page.waitForLoadState('networkidle');
const url = page.url();
expect(url, 'Ha de navegar a la certificació').toContain('/certificacio');

// DESPRÉS:
await certLink.click();
await page.waitForURL('**/certificacio', { timeout: 10000 });
const url = page.url();
expect(url, 'Ha de navegar a la certificació').toContain('/certificacio');
```

Fer el mateix fix al test `'Certificació treballador accessible des de la fitxa'`:

```js
// ABANS:
await certLink.click();
await page.waitForLoadState('networkidle');
const url = page.url();
expect(url, 'Ha de navegar a la certificació del treballador').toContain('/certificacio');

// DESPRÉS:
await certLink.click();
await page.waitForURL('**/certificacio**', { timeout: 10000 });
const url = page.url();
expect(url, 'Ha de navegar a la certificació del treballador').toContain('/certificacio');
```

- [ ] **Step 4.3: Executar els tests corregits**

```bash
cd /tmp/nandu-e2e
npx playwright test final.spec.js --grep "T06" --project=chromium
```

Expected: tots els tests T06 PASS

- [ ] **Step 4.4: Executar la suite completa per confirmar no hi ha regressions**

```bash
npx playwright test final.spec.js --project=chromium
```

Expected: 21/21 PASS (excepte BUG-01 i BUG-02 si no s'ha desplegat el fix a Vercel)

- [ ] **Step 4.5: Commit (si els tests E2E es mouen al projecte)**

```bash
# Opcional: moure tests al projecte
mkdir -p nandu-obres/e2e
cp /tmp/nandu-e2e/tests/final.spec.js nandu-obres/e2e/
cp /tmp/nandu-e2e/playwright.config.js nandu-obres/

git add nandu-obres/e2e/ nandu-obres/playwright.config.js
git commit -m "test: afegir suite E2E Playwright al projecte

Tests cobreixen: login, crear obra, planificació, acta (hores+fotos),
fitxa treballador, certificació per obra i per treballador."
```

---

## Ordre d'execució recomanat

```
Task 1 (BUG-01) → Task 2 (BUG-02) → Task 3 (BUG-03) → Task 4 (BUG-04)
     ↑ CRÍTICO              ↑ HIGH               ↑ LOW               ↑ LOW (test fix)
     ~20 min               ~30 min              ~10 min              ~5 min
```

Total estimat: **~65 minuts**

---

## Self-Review

### Spec coverage
- ✅ BUG-01: Task 1 cobreix `ObraForm` try-catch + error state
- ✅ BUG-02: Task 2 cobreix `guardarActa` return + `NovaActaForm` fase 2 amb fotos
- ✅ BUG-03: Task 3 cobreix centralització `TIPUS_LABELS` amb 'Oficial 1a'
- ✅ BUG-04: Task 4 cobreix fix timing test E2E

### Placeholders scan
- Cap "TODO", "TBD" o "similar a Task N" al pla
- Tot el codi és complet i listo per copiar-enganxar

### Type consistency
- `guardarActa` retorna `Promise<{ acteId: string }>` — usat correctament a `NovaActaForm` (`result.acteId`)
- `TIPUS_LABELS` és `Record<TipusTreballador, string>` — consistent amb l'ús als dos components
- `savedActeId: string | null` — comprovat amb `if (savedActeId)` abans d'usar
